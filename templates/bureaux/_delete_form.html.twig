<form method="post" action="{{ path('app_bureaux_delete', {'id': bureaux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bureaux.id) }}">
    <br>
    <button class="btn btn-danger" style="color:white;font-weight:bold" class="btn">
        Supprimer
    </button>
</form>
